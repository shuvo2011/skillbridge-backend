{
  "version": 2,
  "buildCommand": "pnpm install && npx prisma generate && npx tsc",
  "builds": [
    {
      "src": "dist/app.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/app.js"
    }
  ]
}